<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_camera"
                android:icon="@drawable/ic_menu_camera"
                android:title="Añadir Mi Amiibo"/>
        <item
                android:id="@+id/nav_todos"
                android:icon="@drawable/ic_todos"
                android:title="Todos los Amiibo"/>
        <item
                android:id="@+id/nav_categorias"
                android:icon="@drawable/ic_categorias"
                android:title="Game Series"/>
        <item
                android:id="@+id/nav_fav"
                android:icon="@drawable/ic_fav"
                android:title="Favoritos"/>
        <item
                android:id="@+id/nav_comunity"
                android:icon="@drawable/ic_comunity"
                android:title="Communidad"/>
        <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_misamiibos"
                android:title="Mis Amiibos"/>

    </group>

    <item android:title="">
        <menu>
            <item
                    android:id="@+id/nav_logout"
                    android:icon="@drawable/ic_out"
                    android:title="Logout"/>
        </menu>
    </item>

</menu>
